# 🛡️ Hardening Server Automation (Ansible)

Этот проект предназначен для базовой настройки безопасности (харденинга) сервера на базе Ubuntu/Debian. Сценарий создает нового пользователя, настраивает доступ по SSH-ключам, конфигурирует Firewall (UFW) и защищает сервер от брутфорс-атак (Fail2Ban).

---

## 📋 Подготовка параметров

Для работы скрипта необходимо настроить два основных файла: `inventory.ini` и `security.yml`.

### 1. Настройка `inventory.ini`

Файл инвентаризации содержит данные для первичного подключения к серверу.

| Параметр | Описание |
| --- | --- |
| **`ansible_host`** | Публичный IP-адрес вашего сервера (например, `91.210.171.41`). |
| **`ansible_user`** | Имя пользователя, под которым Ansible зайдет на сервер **в первый раз** (обычно `root` или имя, выданное хостингом). |
| **`ansible_password`** | Пароль от этого пользователя (необходим для первого входа и выполнения `sudo`). |
| **`ansible_become_password`** | Тот же пароль (используется для получения прав `root` внутри скрипта). |
| **`allow_ports`** | Список дополнительных портов, которые нужно открыть в Firewall (например, `[5432, 4000]`). |

**Пример заполнения:**

```ini
[all]
one_server ansible_host=91.210.171.41 ansible_user=root ansible_password=YOUR_PASSWORD ansible_become_password=YOUR_PASSWORD allow_ports=[5432, 4000]

```

---

### 2. Настройка `security.yml` (Блок `vars`)

В самом Playbook необходимо указать данные нового "защищенного" пользователя.

| Переменная | Описание |
| --- | --- |
| **`target_user`** | Имя нового пользователя, которого создаст скрипт (например, `ArturOG`). |
| **`user_ssh_public_key`** | **Важно!** Весь ваш публичный ключ (`ssh-rsa ...`). Должен быть в одну строку. |
| **`ssh_port`** | Порт для SSH (по умолчанию `22`). |

> ⚠️ **Важно про пароль пользователя**: В таске `Create a new user` используется зашифрованный хеш пароля (`password: '$6$...'`). Не вставляйте туда обычный текст!
> Чтобы создать свой хеш, используйте команду: `mkpasswd -m sha-512` или `python3 -c 'import crypt; print(crypt.crypt("ваш_пароль", crypt.mksalt(crypt.METHOD_SHA512)))'`.

---

## 🚀 Запуск

1. **Проверьте доступность сервера:**
```bash
ansible all -i inventory.ini -m ping

```


2. **Запустите настройку безопасности:**
```bash
ansible-playbook -i inventory.ini security.yml

```



---

## 🛡️ Что именно делает этот скрипт?

1. **Пользователь**: Создает нового системного администратора и добавляет его в группу `sudo`.
2. **SSH**: Добавляет ваш публичный ключ и **отключает вход по паролю** (после этого войти можно будет только по ключу).
3. **Kernel**: Отключает ответы на `ping` (ICMP), делая сервер менее заметным для сканеров.
4. **Fail2Ban**: Автоматически банит IP-адреса, которые пытаются подобрать пароль к вашему SSH.
5. **UFW**: Закрывает все входящие порты, кроме SSH и тех, что вы указали в `allow_ports`.

---

## ⚠️ Внимание!

Перед запуском убедитесь, что ваш публичный ключ (`id_rsa.pub`) корректно скопирован. Если вы ошибетесь в ключе, а скрипт отключит вход по паролю, вы **потеряете доступ** к серверу по SSH.

